import styled from "@emotion/styled";

export const DivContainer = styled('div')(({ theme }) => ({
    position: 'relative',
}))

export const DivGrid = styled('div')(({ theme }) => ({
    display: 'inline-block',
    width: '100px',
    margin: "0 3px",
    marginLeft: 'auto',
}))