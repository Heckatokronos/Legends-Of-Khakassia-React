import {
    styled,
} from "@mui/material";

export const ImgStyled = styled('img')(({ theme }) => ({
    margin: '0 auto',
    maxWidth: '100%',
    height: 'auto',
    marginTop: '1.5rem',
    marginBottom: '1rem',
    borderRadius: '1rem'
}))